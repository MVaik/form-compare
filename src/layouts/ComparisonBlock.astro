---
const { title } = Astro.props;
---

<section class="p-2">
  <h2 class="text-2xl border-b-2">{title}</h2>
  <div class="grid grid-cols-[repeat(auto-fit,minmax(180px,1fr))] gap-2">
    <slot />
  </div>
</section>
